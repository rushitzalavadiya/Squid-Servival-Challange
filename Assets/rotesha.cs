using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class rotesha : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }
    public void Farvuse()
    {
        
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
